package com.example.checkmate.domain.point.entity;

public enum LedgerType {
    SIGNUP_BONUS,
    TEST_CHARGE,
    ROOM_STAKE,
    ROOM_SETTLEMENT_REWARD,
    ROOM_SETTLEMENT_REFUND,
    ROOM_SETTLEMENT_SUCCESS_BONUS,
    ROOM_REFUND
}